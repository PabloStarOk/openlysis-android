package com.openlysis.data.remote.di

/**
 * Enum representing the available HTTP clients for remote data operations in the application.
 */
internal enum class AppHttpClient {
    Analysis,
    Authentication
}