package com.openlysis.data.user.model

/**
 * Represents the available theme configurations for the application.
 *
 * - System: Follows the system-wide theme setting.
 * - Light: Uses the light theme.
 * - Dark: Uses the dark theme.
 */
enum class ThemeConfig {
    System,
    Light,
    Dark
}