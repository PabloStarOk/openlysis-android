<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application>

        <receiver android:name=".receiver.AnalyzableSmsReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.openlysis.Analysis.SMS_ANALYSIS_AVAILABLE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>